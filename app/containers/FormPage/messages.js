/*
 * FormPage Messages
 *
 * This contains all the text for the FormPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FormPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormPage container!',
  },
});
