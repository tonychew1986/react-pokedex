import styled from 'styled-components';

const Style = styled.div`
  padding: 20px 20px;

  .attribute {
    padding-bottom: 10px;
  }

  .subtitle {
    font-weight: bold;
  }
`;

export default Style;
